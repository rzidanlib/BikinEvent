import 'package:flutter/material.dart';

class OrganizerSettingsPage extends StatelessWidget {
  const OrganizerSettingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Settings Akun (segera hadir)'));
  }
}
