import 'package:flutter/material.dart';

class OrganizerTicketsPage extends StatelessWidget {
  const OrganizerTicketsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Kelola Tickets (segera hadir)'));
  }
}
