import 'package:flutter/material.dart';

class OrganizerCategoryPage extends StatelessWidget {
  const OrganizerCategoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Kelola Category (segera hadir)'));
  }
}
